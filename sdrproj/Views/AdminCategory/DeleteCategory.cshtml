@model sdrproj.Models.Category
@{
    ViewData["Title"] = "Delete Category";
}
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h3 class="card-title">Delete Category</h3>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>Warning!</strong> Are you sure you want to delete this category?
                    </div>

                    <div class="mb-3">
                        <strong>Category Name:</strong> @Model.Name
                    </div>

                    @if (Model.SubCategories.Any())
                    {
                        <div class="mb-3">
                            <strong>SubCategories:</strong>
                            <ul class="list-group list-group-flush">
                                @foreach (var subcat in Model.SubCategories)
                                {
                                    <li class="list-group-item">@subcat.Name</li>
                                }
                            </ul>
                            <div class="alert alert-danger mt-2">
                                <small>You cannot delete this category because it has subcategories. Please delete the subcategories first.</small>
                            </div>
                        </div>
                    }

                    <form asp-action="DeleteCategory" method="post">
                        <input asp-for="CategoryId" type="hidden" />
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="@Url.Action("Index")" class="btn btn-secondary me-md-2">Cancel</a>
                            @if (!Model.SubCategories.Any())
                            {
                                <button type="submit" class="btn btn-danger">Delete Category</button>
                            }
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
