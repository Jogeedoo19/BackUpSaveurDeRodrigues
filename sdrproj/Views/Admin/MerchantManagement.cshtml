@model IEnumerable<sdrproj.Models.Merchant>

@{
    ViewData["Title"] = "Merchant Management";
}
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">
                        <i class="fas fa-store mr-2"></i>
                        Merchant Management
                    </h3>
                </div>
                <div class="card-body">
                    <!-- Success/Error Messages -->
                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle mr-2"></i>
                            @TempData["Success"]
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    }

                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-circle mr-2"></i>
                            @TempData["Error"]
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    }

                    <!-- Statistics -->
                    <div class="row mb-3">
                        <div class="col-md-4">
                            <div class="info-box">
                                <span class="info-box-icon bg-info"><i class="fas fa-store"></i></span>
                                <div class="info-box-content">
                                    <span class="info-box-text">Total Merchants</span>
                                    <span class="info-box-number">@Model.Count()</span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="info-box">
                                <span class="info-box-icon bg-success"><i class="fas fa-store"></i></span>
                                <div class="info-box-content">
                                    <span class="info-box-text">Active Merchants</span>
                                    <span class="info-box-number">@Model.Count(m => m.Status == "active")</span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="info-box">
                                <span class="info-box-icon bg-danger"><i class="fas fa-store-slash"></i></span>
                                <div class="info-box-content">
                                    <span class="info-box-text">Blocked Merchants</span>
                                    <span class="info-box-number">@Model.Count(m => m.Status == "blocked")</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Merchants Table -->
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="thead-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Profile</th>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Phone</th>
                                    <th>Postal Code</th>
                                    <th>Status</th>
                                    <th>Created At</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var merchant in Model)
                                {
                                    <tr>
                                        <td>@merchant.MerchantId</td>
                                        <td>
                                            @if (!string.IsNullOrEmpty(merchant.ProfileImagePath))
                                            {
                                                <img src="@merchant.ProfileImagePath" alt="Profile" class="rounded-circle" style="width: 40px; height: 40px;">
                                            }
                                            else
                                            {
                                                <div class="rounded-circle bg-secondary d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                    <i class="fas fa-store text-white"></i>
                                                </div>
                                            }
                                        </td>
                                        <td>@merchant.Name</td>
                                        <td>@merchant.Email</td>
                                        <td>@merchant.Phone</td>
                                        <td>@merchant.PostalCode</td>
                                        <td>
                                            @if (merchant.Status == "active")
                                            {
                                                <span class="badge bg-success">
                                                    <i class="fas fa-check-circle mr-1"></i>Active
                                                </span>
                                            }
                                            else if (merchant.Status == "blocked")
                                            {
                                                <span class="badge bg-danger">
                                                    <i class="fas fa-ban mr-1"></i>Blocked
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-secondary">@merchant.Status</span>
                                            }
                                        </td>
                                        <td>@merchant.CreatedAt.ToString("MMM dd, yyyy")</td>
                                        <td>
                                            @if (merchant.Status == "active")
                                            {
                                                <form asp-action="BlockMerchant" asp-route-id="@merchant.MerchantId" method="post" style="display: inline;">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-danger"
                                                            onclick="return confirm('Are you sure you want to block this merchant?')">
                                                        <i class="fas fa-ban mr-1"></i>Block
                                                    </button>
                                                </form>
                                            }
                                            else if (merchant.Status == "blocked")
                                            {
                                                <form asp-action="UnblockMerchant" asp-route-id="@merchant.MerchantId" method="post" style="display: inline;">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-success"
                                                            onclick="return confirm('Are you sure you want to unblock this merchant?')">
                                                        <i class="fas fa-check mr-1"></i>Unblock
                                                    </button>
                                                </form>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    @if (!Model.Any())
                    {
                        <div class="text-center py-4">
                            <i class="fas fa-store fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">No merchants found</h5>
                            <p class="text-muted">There are no merchants registered in the system yet.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Auto-hide alerts after 5 seconds
        setTimeout(function() {
            $('.alert').fadeOut('slow');
        }, 5000);
    </script>
}